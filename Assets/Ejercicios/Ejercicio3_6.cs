using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio3_6 : MonoBehaviour
{
    [SerializeField] private float velocitykm;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    float VelocityMs(float velocityKm)
    {
        float velocityMs=velocityKm;
        return velocityMs;

    }
}
