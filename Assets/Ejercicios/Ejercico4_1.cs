using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercico4_1 : MonoBehaviour
{
    int radio;
    int Base;
    int altura;
    int lado;
    int lado2;
    float pi = 3.14f;
    float circulo;


    // Start is called before the first frame update
    void Start()
    {
        //lcularCirculo(int radio, int pi);

    }

    // Update is called once per frame
    void Update()
    {
       //nt CalcularCirculo(int radio, int pi)
        {

        circulo = radio * 2 * pi;
        Debug.Log(circulo);
        }
    }
}
